apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kuber-ingress-35
spec:
  ingressClassName: nginx
  rules:
  - host: arch.homework
    http:
      paths:
      - path: /order
        pathType: Prefix
        backend:
          service:
            name: kuber-service-35
            port:
              number: 80
